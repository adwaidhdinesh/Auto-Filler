# Local user profile / personal data (never commit real form data)
profile.json

# Node.js (only matters once a build/test step is added)
node_modules/
dist/
coverage/

# Phase 2 build output (generated by `npm run build`)
phase2/dist/

# Editor / OS cruft
.DS_Store
Thumbs.db
*.swp

# Logs
*.log